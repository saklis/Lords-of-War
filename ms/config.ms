// An object holding global configuration for the game
Config = object
  
  // Display debug log information in console
  Debug = true
  
  // Name of the scene that will be loaded as default
  DefaultSceneClass = "CameraTest"
  
  // Max number of entities allowed in the scene
  EntitiesMaxNumber = 1000
end